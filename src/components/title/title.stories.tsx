import { Meta } from '@storybook/react';
import { Title } from '.';

export default {
  title: 'Typography/Title',
  component: Title,
} as Meta;

export const Base = () => <Title>Lorem ipsum dolor</Title>;
